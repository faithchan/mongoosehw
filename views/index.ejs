<!DOCTYPE html>
<html>
<link rel="stylesheet" href="/app.css">

<head>
    <meta charset="utf-8" />
    <title>shop</title>
</head>

<body>
    <h1>Products</h1>
    <div >
        <a href="/product/new">Add product</a>
    </div>
    
    <div >
        <% for (let i=0; i < data.length; i++) { %>
            <div >
                <a href="/product/<%=data[i]._id%>">
                    <%= data[i].name %>
                </a>
                <div >
                    <img src=<%=data[i].img %>>
                </div>
                <h3>Price: $<%= data[i].price%></h3>
                <div >
                    <form action="/product/<%=data[i]._id%>/edit" method="GET">
                        <input type="submit" value="EDIT" />
                    </form>
                    <form action="/product/<%= data[i]._id%>/?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE" />
                    </form>
                </div>
            </div>
            <% } %>
    </div>
</body>

</html>